import "./globals.css";

export const metadata = {
  title: "Synapse Explainer",
  description: "A visual comparison of KV caching and plastic neural memory."
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}

